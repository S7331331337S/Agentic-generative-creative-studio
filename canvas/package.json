{
  "name": "@agcs/canvas",
  "version": "1.0.0",
  "description": "MSTRMND Content Engine CANVAS runtime — portable DAG fan-out, voice gates, and draft tools. Reference implementation; not the Express dashboard.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "lint": "eslint .",
    "clean": "rm -rf dist coverage"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/node": "^26.3.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  }
}
